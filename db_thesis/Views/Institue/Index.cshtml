@model List<Institue>
@if (TempData["basarili"] != null)
{
	<h2 style="color:red">@TempData["basarili"]</h2>
}

<div class="container">

	<div class="row pt-3 row pb-5">
		<div>
			<h1>Institues</h1>
		</div>
	</div>
	<table class="table table-bordered">
		<thead>
			<tr>
				<th>
					No
				</th>
				<th>
					InstitueName
				</th>
				<th>
					UniversityName
				</th>
				<th>
				</th>
				<th>
				</th>
			</tr>
		</thead>
		<tbody>
			
			@foreach (var (institue, index) in Model.Select((value, i) => (value, i)))
			{
				<tr>

					<td>@(index + 1)</td>
					<td>@institue.InstitueName</td>
					<td>@institue.University.UniversityName</td>
				
					
					<td>
						<a asp-controller="Institue" asp-action="EkleGuncelle" asp-route-id="@institue.InstitueId" class="btn btn-success" style="width:150px">
							Update
						</a>

					</td>
					<td>
						<a asp-controller="Institue" asp-action="Sil" asp-route-id="@institue.InstitueId" class="btn btn-danger" style="width:150px">
							Delete
						</a>
					</td>
				</tr>

				
			}
		</tbody>

	</table>

	<div class="row pt-3 pb-2">
		<div>
			<a asp-controller="Institue" asp-action="EkleGuncelle" class="btn btn-lg btn-primary" type="button">
				Create New Institue
			</a>
		</div>
	</div>
					
</div>